# AGENTS

- Reply briefly and clearly.
- Write internal notes in English.
- Write user questions and work reports in Russian.
- Do not start the dev server unless explicitly asked.
- Do not use screenshots to verify visual changes.
- For this project, read `AGENTS.locale.md` first.
- If `AGENTS.locale.md` conflicts with this file, `AGENTS.locale.md` wins.
- If the user says `Prepare for a new chat`, create `docs/project-state.md` with a handoff summary.
- If the user says `Continue the previous chat`, read `AGENTS.md` and `docs/project-state.md`, then continue from there.
- `/prepare-next` means `Prepare for a new chat`.
- `/continue-last` means `Continue the previous chat`.
- If `AGENTS.md` is missing in a project, remind the user to add it.
- Create `AGENTS.locale.md` as soon as possible.
